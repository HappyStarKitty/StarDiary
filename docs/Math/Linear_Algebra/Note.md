# 线性代数

## 线性方程组

$$A x=b$$

## 矩阵

秩（rank）：非零子式的最高阶数

特征值：$A\alpha = \lambda \alpha$

相似矩阵：$B=P^{-1}AP$

实对称矩阵：

- 特征值均为实数
- 不同特征值的特征向量正交
- 可相似对角化
- 可正交相似对角化

二次型

## 向量

正交矩阵：$A A^{T} = E \leftrightarrow A^{-1} = A^{T}$